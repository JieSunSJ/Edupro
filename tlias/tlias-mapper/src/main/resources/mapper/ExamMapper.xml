<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hytc.tlias.mapper.ExamMapper">
    <select id="findByStudentId" resultType="cn.hytc.tlias.entity.Exam">
        SELECT e.*, c.name AS course_name, c.teacher_name
        FROM exam e
        JOIN course c ON e.course_id = c.id
        JOIN student_course sc ON sc.course_id = c.id AND sc.student_id = #{studentId}
        ORDER BY e.exam_date, e.start_time
    </select>
</mapper>