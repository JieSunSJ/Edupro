<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.hytc.tlias.mapper.CourseMapper">
    <select id="listAvailable" resultType="cn.hytc.tlias.entity.Course">
        SELECT c.*,
               (SELECT COUNT(*) FROM student_course sc WHERE sc.course_id = c.id) AS enrolled_count
        FROM course c
        WHERE c.status = 1
        <if test="name != null and name != ''">
            AND c.name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="subject != null">
            AND c.subject = #{subject}
        </if>
        ORDER BY c.create_time DESC
    </select>

    <select id="getById" resultType="cn.hytc.tlias.entity.Course">
        SELECT c.*,
               (SELECT COUNT(*) FROM student_course sc WHERE sc.course_id = c.id) AS enrolled_count
        FROM course c
        WHERE c.id = #{id}
    </select>

    <select id="countEnrolled" resultType="int">
        SELECT COUNT(*) FROM student_course WHERE course_id = #{courseId}
    </select>
</mapper>